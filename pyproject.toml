[project]
name = "google-serp"
version = "0.1.0"
description = "A modular, on-demand Python SERP crawler for Google. Features dynamic browser switching, advanced anti-bot stealth, and structured JSON output."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "black>=26.5.1",
    "pydantic>=2.13.4",
    "seleniumbase>=4.49.2",
]
